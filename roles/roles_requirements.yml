# Role dependencies. To install them, execute:
# ansible-galaxy install -f -r roles_requirements.yml -p external

- src: https://github.com/geerlingguy/ansible-role-git/archive/1.2.1.tar.gz
  name: git-install